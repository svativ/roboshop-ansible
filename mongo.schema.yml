
#echo -e "\e[36m>>>>>>>>>>>>  INstall Mongo Client  <<<<<<<<<<<<\e[0m"  | tee -a /tmp/roboshop.log
#yum install mongodb-org-shell -y &>>${log}
#func_exit_status
#
#echo -e "\e[36m>>>>>>>>>>>>  Load User Schema  <<<<<<<<<<<<\e[0m"  | tee -a /tmp/roboshop.log
#mongo --host mongodb.sdevops99.online </app/schema/${component}.js &>>${log}
#func_exit_status
- name: copy Mongodb yum repo file
  ansible.builtin.copy:
    src: mongodb.repo
    dest: /etc/yum.repos.d/mongodb.repo

- name: Install MongoDB Client
  ansible.builtin.yum:
    name: mongodb-org-shell
    state: installed
- name: load Schema
  ansible.builtin.shell: mongo --host mongodb-dev.sdevops99.online </app/schema/${{component}}

